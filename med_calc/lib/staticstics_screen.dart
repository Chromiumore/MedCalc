import 'package:flutter/material.dart';

class StaticsticsScreen extends StatelessWidget {
  const StaticsticsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Страница статистики'),
      ),
    );
  }
  
}